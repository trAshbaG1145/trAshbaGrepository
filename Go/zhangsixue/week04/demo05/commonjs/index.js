/**
 * data.js
 * 
 * function sum(x, y) {
  return x + y;
}

function concat(str1, str2) {
  return str1 + "," + str2;
}

const LIST = [1, 2, 3];

module.exports = { sum, concat, LIST };

 */

const dataM = require("./data.js");
console.log("dataM", dataM.sum(1,112));

console.log("dataM", dataM);
