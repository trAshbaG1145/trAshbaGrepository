# Ultralytics YOLO 🚀, AGPL-3.0 license
# YOLOv11n-P2-Dilated: 增强版，集成空洞深度卷积
# 在 P2 检测头基础上，通过空洞卷积扩大感受野以更好捕获微小目标上下文信息

# Parameters
nc: 10  # number of classes (VisDrone dataset)
scales:  # model compound scaling constants
  n: [0.50, 0.25, 1024]  # depth_multiple, width_multiple, max_channels

# YOLOv11n-P2-Dilated backbone with Dilated Convolution
backbone:
  # [from, repeats, module, args]
  - [-1, 1, Conv, [64, 3, 2]]   # 0-P1/2
  - [-1, 1, Conv, [128, 3, 2]]  # 1-P2/4  ⚠️ 保留此层用于高分辨率检测
  - [-1, 2, C3k2, [256, False, 0.25]]
  
  # 🆕 在 P3 层引入空洞卷积 (dilation=2)
  - [-1, 1, Conv, [256, 3, 2]]  # 3-P3/8 (下采样)
  - [-1, 1, Conv, [256, 3, 1, 2]]  # 4-Dilated Conv (扩大感受野) 💡
  - [-1, 2, C3k2, [512, False, 0.25]]  # 5
  
  # 🆕 在 P4 层引入空洞卷积 (dilation=2)
  - [-1, 1, Conv, [512, 3, 2]]  # 6-P4/16 (下采样)
  - [-1, 1, Conv, [512, 3, 1, 2]]  # 7-Dilated Conv 💡
  - [-1, 2, C3k2, [512, True]]  # 8
  
  - [-1, 1, Conv, [1024, 3, 2]]  # 9-P5/32
  - [-1, 2, C3k2, [1024, True]]
  - [-1, 1, SPPF, [1024, 5]]  # 11

# YOLOv11n-P2-Dilated head with 4 detection layers
head:
  - [-1, 1, nn.Upsample, [None, 2, 'nearest']]
  - [[-1, 8], 1, Concat, [1]]  # cat backbone P4 (注意索引调整)
  - [-1, 2, C3k2, [512, False]]  # 14

  - [-1, 1, nn.Upsample, [None, 2, 'nearest']]
  - [[-1, 5], 1, Concat, [1]]  # cat backbone P3
  - [-1, 2, C3k2, [256, False]]  # 17 (P3/8-small)

  # ========== P2 高分辨率检测分支 (核心改进) ==========
  - [-1, 1, nn.Upsample, [None, 2, 'nearest']]  # 上采样到 P2 尺度
  - [[-1, 2], 1, Concat, [1]]  # cat backbone P2 (融合浅层特征)
  - [-1, 2, C3k2, [128, False]]  # 20 (P2/4-tiny) 💡 微小目标检测头
  # ===================================================

  - [-1, 1, Conv, [128, 3, 2]]
  - [[-1, 17], 1, Concat, [1]]  # cat head P3
  - [-1, 2, C3k2, [256, False]]  # 23 (P3/8-small)

  - [-1, 1, Conv, [256, 3, 2]]
  - [[-1, 14], 1, Concat, [1]]  # cat head P4
  - [-1, 2, C3k2, [512, False]]  # 26 (P4/16-medium)

  - [-1, 1, Conv, [512, 3, 2]]
  - [[-1, 11], 1, Concat, [1]]  # cat head P5
  - [-1, 2, C3k2, [1024, True]]  # 29 (P5/32-large)

  - [[20, 23, 26, 29], 1, Detect, [nc]]  # Detect(P2, P3, P4, P5)

# 🔍 改进说明:
# 1. Backbone 层 4 和 7: 插入空洞卷积 (kernel=3, stride=1, dilation=2)
# 2. 空洞卷积作用: 在不增加参数的情况下扩大感受野，捕获更多上下文信息
# 3. 适用场景: 微小目标周围的背景信息对识别至关重要 (如拥挤场景中的行人)
# 4. 参数增加: 约 +0.1M (相对于 P2 版本)
