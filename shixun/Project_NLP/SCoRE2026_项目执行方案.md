# SCoRE2026 项目执行方案

> **任务：** CCL 2026 任务十 —— 基于情景的常识推理评测（SCoRE）
> **主办方：** 北京大学 + 华为
> **难度等级：** B级（中高难度，20/30分）
> **方案版本：** v2.0（CoT 推理路线）

---

## 一、任务概述

SCoRE2026 旨在评估大语言模型在常识场景下的复杂逻辑推理能力。核心挑战是将自然语言描述的情景转化为逻辑推理，经过多步推导选择正确答案。

### 1.1 五类推理任务

| 类型 | 核心能力 | 训练集数量 | 测试集分布 |
|------|---------|-----------|-----------|
| **时间推理** | 事件时间线重建、相对时间偏移计算 | 1000 | 单域100 + time+social:200, time+nature:200 |
| **空间推理** | 多物体空间布局推理、方位关系推导 | 1000 | 单域100 + space+social:200, space+nature:75 |
| **自然推理** | 物体属性分类、排除法匹配 | 1000 | 单域100 + time+nature:200, space+nature:75 |
| **社会推理** | 亲属/职场关系网络推导 | 500 | 单域25 + time+social:200, space+social:200 |
| **融合推理** | 跨域联合约束求解 | 100 | 融合域共675 |

### 1.2 关键约束条件

| 约束项 | 要求 |
|--------|------|
| **模型规模** | Dense模型 ≤ 8B；MoE模型总参 ≤ 30B，每token激活 ≤ 3B |
| **外部数据** | 禁止使用SCoRE2026以外的任何数据集 |
| **测试数据** | 禁止用于训练、微调或作为prompt示例 |
| **评估指标** | Accuracy = 正确数 / 总题数 |
| **提交格式** | `[{"id": "SCoRE2026-test-N", "answers": ["A", "B"]}]` |

---

## 二、核心挑战分析

### 🔴 挑战1：训练-测试域偏移（最关键）

```
训练集分布：               测试集分布：
┌──────────────┐          ┌────────────────────┐
│ 单域 97.2%   │    →     │ 融合域 67.5%        │
│ 融合域 2.8%  │          │ 单域 32.5%          │
└──────────────┘          └────────────────────┘
```

训练集几乎全是单域问题，测试集以融合域为主。CoT 推理链天然具有跨域泛化能力——推理过程不依赖领域特定的结构化 Schema。

### 🟠 挑战2：多步约束求解

每个问题本质上是一个约束满足问题（CSP），需要从自然语言中识别约束、构建逻辑关系、逐步推理。CoT 路线将这一过程交给 LLM 端到端完成。

### 🟡 挑战3：双语推理

中英文各约一半，模型需同时掌握两种语言的推理模式。

### 🟢 挑战4：多答案问题

23.6% 的题目有多个正确答案，需要模型判断哪些选项同时满足条件。

---

## 三、技术方案

### 3.1 总体架构：CoT 推理 + 知识蒸馏

```
训练阶段:
  训练集(3600) → [DeepSeek-V3 CoT推理] → 过滤(仅保留正确推理) → [LoRA微调 Qwen2.5-7B]

推理阶段:
  测试集(1000) → [微调后 Qwen2.5-7B] → CoT推理链 + 答案JSON
```

### 3.2 为什么选择 CoT 方案

| Neuro-Symbolic（已放弃） | CoT 推理（当前方案） |
|--------------------------|---------------------|
| LLM 提取约束 JSON → 符号求解器 | LLM 端到端 CoT 推理 |
| 约束提取正确率仅 3.6%（DeepSeek-V3） | Teacher 零样本 74.1% |
| 需要精确的结构化输出 | LLM 擅长的自然语言推理 |
| 符号求解器理论上 100% 正确 | 推理链可解释、可验证 |
| 对融合域泛化依赖求解器设计 | 推理链天然跨域 |

### 3.3 为什么放弃 Neuro-Symbolic

经实验验证，DeepSeek-V3 在"自然语言→结构化约束JSON"任务上的正确率仅 3.6%。失败模式包括：
- 实体名称不精确（"跑步" vs "他跑步"）
- 约束偏移量/方向错误
- 遗漏关键约束
- 将非事件描述误识别为事件

CoT 路线避免了中间结构化表示的精度损失——LLM 直接用自然语言推理，输出的正确率（74.1%）远高于约束提取（3.6%）。

### 3.4 模型选择

| 模型 | 参数 | 用途 |
|------|------|------|
| **DeepSeek-V3** (API) | — | Teacher：生成 CoT 训练数据 |
| **Qwen2.5-7B-Instruct** | 7B | Student：LoRA 微调后推理 |

### 3.5 训练数据

| 指标 | 数值 |
|------|------|
| 原始训练集 | 3600 条 |
| Teacher 零样本正确率 | 74.1% |
| 过滤后训练数据 | 2668 条（仅保留正确推理链） |
| 数据格式 | ChatML（`<|im_start|>system/user/assistant`） |
| 推理链格式 | `## Reasoning` + `## Answer {"answers": ["A","B"]}` |

### 3.6 微调配置

```yaml
模型: Qwen2.5-7B-Instruct
训练框架: Transformers + PEFT
训练策略:
  - LoRA微调 (r=16, alpha=32)
  - 学习率: 2e-4
  - Batch size: 4 × 4 accumulation = 16 effective
  - Epochs: 3
  - Max length: 2048
  - 精度: bf16 (Ampere+) / fp16 (Volta)
训练数据: 2668 条 CoT 推理链
验证集: 10% (267 条)
```

---

## 四、实施流程

### Phase 1：CoT 数据生成 ✅

```
已完成:
├── generate_cot_data.py      # DeepSeek API 批量调用
├── CoT prompt 设计           # ## Reasoning + ## Answer 格式
├── 断点续传缓存              # RLock 安全的多线程缓存
└── 全量生成                  # 3600→2668 条, 74.1% 正确率
```

### Phase 2：模型微调 🔄

```
进行中:
├── run_train.py              # 云平台一键训练
├── 模型加载                  # ModelScope / HF 双通道
├── LoRA 微调 (3 epochs)     # 在云端 GPU 上运行
└── 输出: checkpoints/cot_model/final/
```

### Phase 3：推理与提交 ⏳

```
待完成:
├── 测试集推理                # 1000 条 CoT 推理
├── 答案提取                  # 多策略 JSON 解析
└── 输出: outputs/submission_cot.json
```

### Phase 4：消融实验 ⏳

| 实验 | 配置 | 目的 |
|------|------|------|
| 基线 | Qwen2.5-7B 零样本 | 确定难度下限 |
| 模板 | 模板解析器 + 符号求解器 | N-S 路线性能参考 (~5%) |
| CoT 微调 | Qwen2.5-7B LoRA 微调 | 验证知识蒸馏效果 |
| Teacher | DeepSeek-V3 CoT 零样本 | 性能上界 (74.1%) |

---

## 五、风险与对策

| 风险 | 概率 | 影响 | 对策 |
|------|------|------|------|
| 微调后模型性能不佳 | 低 | 高 | DeepSeek-V3 零样本 74.1% 证明 CoT 可行；调参空间大 |
| 融合域泛化不足 | 中 | 中 | CoT 推理链天然跨域；可针对性数据增强 |
| 推理格式不稳定 | 低 | 中 | 多策略答案提取（JSON→regex→回退） |
| GPU 资源不足 | 中 | 高 | 已适配多种云平台；V100 16GB可跑 |
| 时间不足 | 低 | 高 | 代码+数据已完成，仅待 GPU 训练 |

---

## 六、交付物清单

- [x] CoT 数据生成脚本
- [x] LoRA 微调脚本 + 云平台一键训练
- [x] CoT 推理脚本 + 提交生成
- [x] CoT 训练数据（2668 条推理链）
- [x] 项目文档（本方案 + 进度报告 + 实施计划）
- [ ] 微调后模型权重
- [ ] 测试集提交 JSON
- [ ] 消融实验报告
- [ ] 技术报告（4 页 CCL 格式）

### 保留的旧路线产出（消融实验用）

- [x] 约束 Schema + 5 领域符号求解器
- [x] 模板解析器基线（~5%）
- [x] 约束标注 Pipeline

---

> 📅 创建日期：2026年7月3日
> 📝 方案版本：v2.0（2026年7月4日更新，Neuro-Symbolic→CoT）
